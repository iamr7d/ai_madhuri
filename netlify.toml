[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  PYTHON_VERSION = "off"
  CI = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
